// Removed: this file covered createStandaloneTask's "بدون مشروع" fake-project
// wrapper, which no longer exists now that the "project" concept is gone
// entirely from the app. See tests/secretary-standalone-task.test.mjs for the
// replacement coverage (every task is now flat/standalone by default).
